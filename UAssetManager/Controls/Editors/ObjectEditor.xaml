<UserControl x:Class="UAssetManager.Controls.Editors.ObjectEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">
        <!-- Input Panel -->
        <StackPanel Orientation="Horizontal">
			<TextBox x:Name="IndexTextBox" MinWidth="200" TextChanged="OnIndexTextChanged" />
            <Button x:Name="BrowseButton" Width="50" Height="25" Margin="2,0,0,0" Click="OnBrowseClick"
                    Content="{DynamicResource Text.Browse}" ToolTip="{DynamicResource Tooltip.BrowseObject}" />
        </StackPanel>

        <!-- Path Display -->
        <TextBlock x:Name="PathTextBlock" FontStyle="Italic" Foreground="Gray" Margin="0,2,0,0" TextWrapping="Wrap" 
                   PreviewMouseRightButtonDown="OnPreviewMouseRightButtonDown" />
    </StackPanel>
</UserControl>