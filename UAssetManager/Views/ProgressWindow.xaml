<Window x:Class="UAssetManager.Views.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Progress" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Progress Label -->
        <Label Grid.Row="0" x:Name="ProgressLabel" Content="Processing..." 
               HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Progress Bar -->
        <ProgressBar Grid.Row="1" x:Name="ProgressBar" Height="20" 
                     Minimum="0" Maximum="100" Value="0"
                     Margin="0,0,0,10"/>

        <!-- Progress Text -->
        <TextBlock Grid.Row="2" x:Name="ProgressText" Text="0/100" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Cancel Button -->
        <Button Grid.Row="2" Content="Cancel" Width="80" Height="30" 
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Click="Cancel_Click"/>
    </Grid>
</Window>
