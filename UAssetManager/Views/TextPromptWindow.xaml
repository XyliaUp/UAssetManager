<Window x:Class="UAssetManager.Views.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource TextPromptWindow.Title}" Height="180" Width="420"
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content Area -->
        <StackPanel Grid.Row="0" Margin="20">
            <Label x:Name="PromptLabel" Content="{DynamicResource TextPromptWindow.Prompt}" Margin="0,0,0,10" />
            <TextBox x:Name="InputTextBox" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     Height="23" Margin="0,0,0,20" VerticalContentAlignment="Center" />
        </StackPanel>

        <!-- Button Panel -->
        <Border Grid.Row="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{DynamicResource Text.OK}" Style="{StaticResource ButtonStyle}" Click="OK_Click" IsDefault="True"/>
                <Button Content="{DynamicResource Text.Cancel}" Style="{StaticResource ButtonStyle}" Click="Cancel_Click" IsCancel="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>