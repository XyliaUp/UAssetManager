<Window x:Class="UAssetManager.Views.ObjectReferenceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:UAssetManager.Resources"
        Title="{DynamicResource ObjectReferenceDialog.Title}" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="{DynamicResource ObjectReferenceDialog.Search}" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Grid.Column="1" x:Name="SearchTextBox" Width="200" TextChanged="SearchTextBox_TextChanged" ToolTip="{DynamicResource ObjectReferenceDialog.SearchTooltip}"/>
            <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0" 
                       Text="{Binding ItemsSource.Count,ElementName=ObjectsListBox,Converter={StaticResource StringConverter},ConverterParameter=ObjectReferenceDialog.CountFormat}" />
            <Button Grid.Column="3" Content="{DynamicResource ObjectReferenceDialog.AddReference}" Width="100" Height="25" HorizontalAlignment="Right"
                    Click="AddReferenceButton_Click" ToolTip="{DynamicResource ObjectReferenceDialog.AddReferenceTooltip}"/>
        </Grid>

        <!-- Object List -->
        <ListBox x:Name="ObjectsListBox" Grid.Row="1" Margin="10,0,10,0"
                 SelectionMode="Single" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 MouseDoubleClick="OKButton_Click">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="5,3" BorderThickness="0,0,0,1" BorderBrush="#E0E0E0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding PackageIndex}" VerticalAlignment="Center" FontWeight="Bold" Foreground="#0066CC"/>
                            <TextBlock Grid.Column="1" Text="{Binding Path}" VerticalAlignment="Center" Margin="5,0,0,0" TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="{DynamicResource Text.Cancel}" Width="80" Height="25" Click="CancelButton_Click" IsCancel="True" Margin="0,0,10,0" />
            <Button Content="{DynamicResource Text.OK}" Width="80" Height="25" Click="OKButton_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>